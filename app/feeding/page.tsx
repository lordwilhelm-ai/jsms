import AdminShell from "@/app/components/AdminShell";

export default function FeedingPage() {
  return (
    <AdminShell
      sectionLabel="Software Module"
      title="Feeding"
      subtitle="This module page is ready for the next build step."
    >
      <div
        style={{
          background: "#fff",
          borderRadius: "24px",
          padding: "24px",
          boxShadow: "0 14px 35px rgba(0,0,0,0.08)",
        }}
      >
        Feeding page coming next.
      </div>
    </AdminShell>
  );
}